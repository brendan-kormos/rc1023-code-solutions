/* exported getFirstChar */
// define function named getFirstChar with one string parameter named string
//   index the first character of the string and return that value
function getFirstChar(string) {
  return string[0];
}
