/* exported getLastChar */
// define a function named getLastChar with one string parameter named string
//  index the last character of the string and return that value
function getLastChar(string) {
  return string[string.length - 1];
}
