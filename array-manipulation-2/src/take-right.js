/* exported takeRight */
//  define a function named takeRight with 2 parameters, array and count
//    copy the specified number of elements from right to left and return that

function takeRight(array, count) {
  return array.slice(-count);
}
