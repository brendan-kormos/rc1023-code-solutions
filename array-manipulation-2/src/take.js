/* exported take */
// define a function named take with 2 parameters, array, count
//    copy the specified number of elements over to another array and return that

function take(array, count) {
  return array.slice(0, count);
}
