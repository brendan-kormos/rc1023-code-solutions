# postgres-into-quiz-notes

## Quiz Questions

After completing this exercise, you should be able to discuss or answer the following questions:

- Why do we use databases in Web development?
  to be capable of storing data to be used by an application later. cloud databases make it incredibly easy to use data wherever you are
- What is PostgreSQL and what are some alternative relational databases?
  A relational database management system. others include, MySql, SQL by microsoft, Oracle by Oracle Corporation.
- What are some advantages of learning a relational database?
  Those who learn relational databases tend to be more equipped to design non relational databased as well.
- What is one way to see if PostgreSQL is running?

## Notes

All student notes should be written here.

How to write `Code Examples` in markdown

for JS:

```javascript
const data = 'Howdy';
```

for HTML:

```html
<div>
  <p>This is text content</p>
</div>
```

for CSS:

```css
div {
  width: 100%;
}
```
