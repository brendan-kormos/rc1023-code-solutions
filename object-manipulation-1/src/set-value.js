/* exported setValue */
//  define a function named setValue with 3 parameters, object, key, value
//    assign value to the key property of object
function setValue(object, key, value) {
  object[key] = value;
}
